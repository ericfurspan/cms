{
  "host": "${process.env.HOST || '0.0.0.0'}",
  "port": "${process.env.PORT || 8081}",
  "production": true,
  "proxy": {
    "enabled": true,
    "port": 8081
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false,
    "host": "${process.env.HOST || '0.0.0.0'}",
    "port": "${process.env.PORT || 8081}"
  }
}
